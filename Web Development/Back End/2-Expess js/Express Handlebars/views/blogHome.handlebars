<div class="container mt-4">
    <div class="d-flex align-items-center mb-4">
        <h1 class="display-4 mb-0">📝 Blog Posts</h1>
        <span class="badge bg-secondary ms-3">{{blogs.length}} posts</span>
    </div>
    
    {{#if blogs}}
        <div class="row">
            {{#each blogs}}
                <div class="col-md-6 mb-4">
                    <div class="card h-100">
                        <div class="card-body">
                            <div class="d-flex align-items-start mb-2">
                                <span class="me-2">
                                    {{#if (eq this.slug "python-learn")}}🐍{{/if}}
                                    {{#if (eq this.slug "HTML-learn")}}🌐{{/if}}
                                    {{#if (eq this.slug "CSS-learn")}}🎨{{/if}}
                                    {{#if (eq this.slug "JS-learn")}}⚡{{/if}}
                                </span>
                                <h5 class="card-title mb-0">{{this.title}}</h5>
                            </div>
                            <p class="card-text text-muted">{{this.content}}</p>
                        </div>
                        <div class="card-footer bg-transparent border-top">
                            <a href="/blogpost/{{this.slug}}" class="btn btn-primary">📖 Read More</a>
                        </div>
                    </div>
                </div>
            {{/each}}
        </div>
    {{else}}
        <div class="alert alert-info">
            <h4>📭 No blog posts available</h4>
            <p>Check back later for new content!</p>
        </div>
    {{/if}}
</div>
